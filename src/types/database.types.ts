/**
 * @deprecated Import from `@/types/supabase` instead.
 * Thin re-export so any lingering imports resolve to one Database type.
 */
export type {
  Json,
  Database,
  Tables,
  TablesInsert,
  TablesUpdate,
  Enums,
  CompositeTypes,
  Gear,
  GearState,
  GearRequest,
  GearRequestGear,
  GearWithState,
  GearRequestWithDetails,
} from './supabase'
export { Constants } from './supabase'
